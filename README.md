# Информационная система для записи пациентов в поликлинику

## Описание проекта


Данный проект представляет собой кроссплатформенную информационную систему для автоматизации записи пациентов на прием в поликлинику, хранения справочной информации о врачах и услугах, а также ведения медицинских записей по оформленным приемам.

Изначально проект был реализован как WPF-приложение под Windows, однако в текущей версии был переработан в веб-приложение, которое может запускаться на разных операционных системах, включая macOS, Windows и Linux. Это позволяет использовать систему через браузер без привязки к конкретной платформе.

Проект ориентирован на демонстрацию основных принципов проектирования базы данных, построения клиент-серверного взаимодействия и реализации типовых операций медицинской регистрационной системы.

## Тема курсового проекта

Тема проекта:

`Разработка базы данных и информационной системы для регистрации пациентов в поликлинике`

## Цель проекта

Целью проекта является разработка информационной системы, которая позволяет:

- хранить данные о пациентах;
- хранить данные о поликлиниках;
- хранить данные о врачах и их специализациях;
- хранить перечень медицинских услуг;
- формировать доступные слоты записи к врачу;
- записывать пациента на прием;
- искать существующие записи;
- вести медицинские записи по уже созданным приемам.

## Основные задачи проекта

Для достижения цели в проекте были поставлены и реализованы следующие задачи:

1. Выполнить анализ предметной области.
2. Выделить основные сущности и их связи.
3. Спроектировать нормализованную базу данных PostgreSQL.
4. Реализовать серверную часть для работы с базой данных.
5. Реализовать клиентскую часть в виде веб-интерфейса.
6. Реализовать операции добавления, просмотра, поиска и обновления данных.
7. Обеспечить проверку пользовательского ввода.
8. Подготовить систему к демонстрации и защите проекта.

## Предметная область

Предметной областью является работа поликлиники с пациентами и медицинским персоналом.

В рамках системы моделируются следующие процессы:

- регистрация пациента в системе;
- выбор поликлиники;
- выбор специализации врача;
- выбор конкретного врача;
- выбор медицинской услуги;
- выбор доступной даты и времени приема;
- оформление записи;
- поиск уже созданной записи;
- ведение медицинской записи по приему.

## Актуальность проекта

Актуальность проекта обусловлена тем, что в реальных медицинских организациях процесс записи на прием требует хранения и обработки большого объема взаимосвязанных данных:

- персональных данных пациента;
- расписания врачей;
- информации о поликлиниках;
- списка медицинских услуг;
- истории оформленных приемов;
- медицинских записей по обращениям.

Использование базы данных и единого интерфейса позволяет:

- уменьшить количество ошибок при записи;
- ускорить поиск нужной информации;
- структурировать данные;
- упростить работу регистратора;
- сделать систему более удобной для демонстрации логики предметной области.

## Архитектура решения

Текущая версия проекта состоит из трех основных частей:

1. База данных PostgreSQL.
2. Серверная часть на Node.js.
3. Клиентская часть на HTML, CSS и JavaScript.

### Серверная часть

Серверная часть реализована в файле [server.js](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app/server.js).

Она отвечает за:

- запуск локального HTTP-сервера;
- обработку API-запросов;
- взаимодействие с PostgreSQL;
- получение данных для интерфейса;
- добавление пациентов;
- оформление записи на прием;
- добавление врача, услуги и доступного слота;
- получение списка всех записей;
- создание и обновление медицинских записей.

### Клиентская часть

Клиентская часть находится в папке [web_app/public](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app/public).

Основные файлы:

- [index.html](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app/public/index.html) — структура интерфейса;
- [styles.css](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app/public/styles.css) — оформление интерфейса;
- [app.js](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app/public/app.js) — логика работы интерфейса, загрузка данных, валидация и взаимодействие с API.

### База данных

Схема базы данных описана в файле [database_schema.sql](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/database_schema.sql).

Тестовые данные для демонстрации находятся в файле [seed.sql](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app/db/seed.sql).

## Используемые технологии

В проекте используются следующие технологии:

- `PostgreSQL` — система управления базами данных;
- `Node.js` — серверная платформа;
- `HTML5` — структура интерфейса;
- `CSS3` — стилизация интерфейса;
- `JavaScript` — клиентская логика и валидация;
- `psql` — взаимодействие сервера с PostgreSQL;
- `npm` — запуск и обслуживание веб-версии проекта.

## Структура проекта

Основные каталоги и файлы:

- [README.md](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/README.md) — описание проекта;
- [database_schema.sql](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/database_schema.sql) — схема базы данных;
- [web_app](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app) — кроссплатформенная веб-версия проекта;
- [web_app/server.js](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app/server.js) — backend;
- [web_app/public/index.html](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app/public/index.html) — интерфейс;
- [web_app/public/styles.css](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app/public/styles.css) — стили;
- [web_app/public/app.js](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app/public/app.js) — клиентская логика;
- [web_app/db/seed.sql](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app/db/seed.sql) — демонстрационные данные;
- [web_app/scripts/init-db.sh](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app/scripts/init-db.sh) — инициализация базы данных;
- [kursach_src/WpfApp1](/Users/marinaazizova/myprojects/kursach_bd/kursovaya/kursach_src/WpfApp1) — исходная старая WPF-версия проекта.

## Описание базы данных

### Сущности

В проекте реализована нормализованная модель базы данных, включающая 9 сущностей:

1. `patient` — хранит данные о пациентах.
2. `policlinic` — хранит данные о поликлиниках.
3. `specialization` — хранит справочник медицинских специализаций.
4. `doctor` — хранит данные о врачах.
5. `service` — хранит список медицинских услуг.
6. `doctor_service` — связывает врача с услугой и стоимостью.
7. `doctor_availability` — хранит доступные слоты записи к врачу.
8. `appointment` — хранит записи пациентов на прием.
9. `medical_record` — хранит медицинские записи по приемам.

### Причины выбора такой структуры

Выделение отдельных сущностей необходимо для устранения избыточности и для корректного отражения предметной области.

Например:

- одна поликлиника может содержать много врачей;
- один врач относится к одной специализации;
- один врач может оказывать несколько услуг;
- одна услуга может оказываться разными врачами;
- один пациент может иметь несколько записей на прием;
- один прием может иметь одну медицинскую запись.

### Почему поликлиника вынесена в отдельную сущность

Сущность `policlinic` выделена отдельно, потому что:

- врач работает в конкретной поликлинике;
- одна поликлиника связана с несколькими врачами;
- пациент выбирает не только врача, но и медицинскую организацию;
- данные о поликлинике не должны дублироваться в каждой записи врача;
- это позволяет хранить адрес и телефон учреждения централизованно.

## Связи между сущностями

В проекте реализованы следующие основные связи:

- `policlinic 1:M doctor`
- `specialization 1:M doctor`
- `doctor 1:M doctor_availability`
- `doctor M:N service` через таблицу `doctor_service`
- `patient 1:M appointment`
- `doctor_service 1:M appointment`
- `appointment 1:1 medical_record`

## Нормализация базы данных

База данных приведена к нормализованному виду.

Основные признаки нормализации:

- данные о пациентах, врачах, поликлиниках и услугах разделены по отдельным таблицам;
- исключено лишнее дублирование строковых данных;
- связи многие-ко-многим вынесены в отдельную таблицу;
- медицинская запись хранится отдельно от самой записи на прием;
- справочники вынесены в отдельные сущности.

## Функциональные возможности системы

Текущая версия веб-приложения поддерживает следующие функции.

### 1. Регистрация пациента

Пользователь может:

- ввести ФИО пациента;
- указать, есть ли отчество;
- указать возраст;
- указать адрес;
- указать телефон;
- указать дату рождения;
- сохранить пациента в базе данных.

### 2. Оформление записи на прием

Пользователь может:

- выбрать пациента из базы;
- выбрать поликлинику;
- выбрать специализацию;
- выбрать врача;
- выбрать услугу;
- выбрать доступный слот;
- оформить запись на прием.

### 3. Поиск записи

Пользователь может:

- выбрать пациента;
- выбрать услугу;
- получить найденную запись;
- посмотреть статус записи;
- увидеть врача, поликлинику и дату приема.

### 4. Просмотр всех записей

Пользователь может:

- открыть список всех текущих записей;
- посмотреть пациента, врача, услугу, дату и статус;
- выбрать нужную запись для дальнейшей работы.

### 5. Ведение медицинской записи

Для выбранного приема можно:

- указать диагноз;
- указать лечение;
- добавить примечания;
- сохранить медицинскую запись в таблицу `medical_record`.

### 6. Администрирование справочников

Можно добавить:

- новую поликлинику;
- нового врача;
- специализацию;
- услугу;
- стоимость услуги;
- доступную дату и время приема.

## Валидация данных

В системе реализована валидация пользовательского ввода.

Проверяются:

- ФИО пациента;
- ФИО врача;
- наличие или отсутствие отчества;
- обязательность заполнения полей;
- возраст;
- телефон;
- стоимость услуги;
- дата рождения;
- будущая дата доступности;
- выбор сущностей из справочников.

### Примеры проверок

- если ФИО заполнено не полностью, система выводит сообщение о необходимости заполнить поле до конца;
- если отчество отсутствует, пользователь может поставить галочку `Нет отчества`;
- телефон должен содержать корректное количество цифр;
- возраст должен быть целым числом в допустимом диапазоне;
- дата рождения не может быть в будущем;
- дата доступности врача должна быть будущей;
- нельзя оформить запись без выбора врача, услуги и времени.

## Интерфейс системы

Интерфейс разделен на логические вкладки:

1. `Регистратура`
2. `Все записи`
3. `Поиск записи`
4. `Пациенты`
5. `Администрирование`

Такое разделение позволяет:

- отделить рабочие сценарии;
- упростить навигацию;
- сделать проект понятнее для демонстрации;
- показать преподавателю разные части бизнес-логики.

## API веб-приложения

Серверная часть предоставляет набор HTTP API.

Основные маршруты:

- `GET /api/health` — проверка работоспособности сервера;
- `GET /api/dashboard` — статистика по системе;
- `GET /api/patients` — список пациентов;
- `GET /api/policlinics` — список поликлиник;
- `GET /api/specializations` — список специализаций;
- `GET /api/doctors` — список врачей;
- `GET /api/services` — список услуг;
- `GET /api/availability` — доступные слоты врача;
- `GET /api/appointments` — список всех записей;
- `GET /api/appointments/search` — поиск записи;
- `GET /api/medical-records` — получение медзаписи по приему;
- `POST /api/patients` — добавление пациента;
- `POST /api/admin/doctor` — добавление врача, услуги и слота;
- `POST /api/appointments` — создание записи на прием;
- `POST /api/medical-records` — создание или обновление медзаписи.

## Демонстрационные данные

Для удобства показа проекта в системе предусмотрены тестовые данные:

- поликлиники;
- специализации;
- врачи;
- услуги;
- пациенты;
- доступные даты приема;
- тестовые записи;
- тестовая медицинская запись.

Это позволяет запускать систему и сразу проводить демонстрацию без ручного заполнения всех таблиц с нуля.

## Инструкция по запуску

### Что нужно для запуска

Перед запуском проекта на компьютере должны быть установлены:

- `git`
- `node.js`
- `npm`
- `PostgreSQL`
- утилита `psql`

### Команды для запуска в терминале

Ниже приведена последовательность команд, которые нужно ввести в терминале после клонирования репозитория:

```bash
git clone <ссылка-на-репозиторий>
cd kursovaya/web_app
npm run init-db
npm start
```

После запуска нужно открыть в браузере:

```text
http://127.0.0.1:3000
```

### Пошаговый запуск на другом компьютере

Если проект уже скачан или открыт на другом компьютере, нужно выполнить следующие команды:

1. Перейти в папку веб-приложения:

```bash
cd kursovaya/web_app
```

2. Создать и заполнить базу данных:

```bash
npm run init-db
```

3. Запустить сервер:

```bash
npm start
```

4. Открыть приложение в браузере по адресу:

```text
http://127.0.0.1:3000
```

### Полная последовательность команд одним блоком

Если нужен короткий вариант без пояснений, в терминал вводятся такие команды:

```bash
git clone <ссылка-на-репозиторий>
cd kursovaya/web_app
npm run init-db
npm start
```

### Если репозиторий уже клонирован

Если папка проекта уже есть на компьютере, тогда достаточно выполнить:

```bash
cd /путь/до/kursovaya/web_app
npm run init-db
npm start
```

Пример для текущего проекта на этой машине:

```bash
cd /Users/marinaazizova/myprojects/kursach_bd/kursovaya/web_app
npm run init-db
npm start
```

### Что делают команды

Команда:

```bash
npm run init-db
```

выполняет:

- создание базы данных `hosdb`, если она отсутствует;
- применение схемы базы данных из `database_schema.sql`;
- загрузку тестовых данных из `web_app/db/seed.sql`.

Команда:

```bash
npm start
```

выполняет:

- запуск локального Node.js сервера;
- подключение к базе данных PostgreSQL;
- публикацию веб-интерфейса для работы через браузер.

### Если проект запускается на другом компьютере

Для запуска на другом компьютере необходимо:

1. Убедиться, что установлен PostgreSQL.
2. Убедиться, что установлен Node.js.
3. Убедиться, что команда `psql` доступна в терминале.
4. Выполнить `npm run init-db`.
5. Выполнить `npm start`.

Если PostgreSQL установлен корректно, веб-версия проекта запускается без привязки к конкретной операционной системе и может быть использована на macOS, Windows и Linux.

## Преимущества текущей веб-версии

По сравнению с исходной WPF-версией, веб-версия обладает рядом преимуществ:

- запускается на macOS;
- запускается на Windows;
- запускается на Linux;
- не требует Visual Studio;
- не требует WPF;
- может быть показана через обычный браузер;
- проще для демонстрации на защите.

## Что можно говорить на защите

На защите можно делать акцент на следующих моментах:

- предметная область была проанализирована и разложена на сущности;
- база данных построена в нормализованной форме;
- реализованы справочники и рабочие таблицы;
- система поддерживает не только запись на прием, но и медицинскую запись по обращению;
- проект был адаптирован под кроссплатформенный запуск в браузере;
- реализована валидация пользовательского ввода;
- интерфейс разделен по бизнес-сценариям.

## Как переделать текущий PDF-отчет

Ниже приведены рекомендации не для написания нового отчета с нуля, а для правки уже существующей PDF-версии `курсовая.pdf`.

### Что можно оставить почти без изменений

- Страница 1, титульный лист:
  - можно оставить общую структуру титула;
  - нужно только обновить ФИО, группу, год и при необходимости формулировку темы.
- Страница 2, оглавление:
  - можно оставить сам принцип структуры: `Введение`, `Проектирование`, `Разработка`, `Заключение`, `Список источников`;
  - нужно обновить названия подразделов под текущую веб-версию проекта.
- Страница 3, введение:
  - общую идею автоматизации записи пациентов можно оставить;
  - цель, актуальность и задачи уже близки к вашей теме, но формулировки надо немного освежить.
- Страница 4, начало описания предметной области:
  - можно оставить абзацы про пациентов, врачей, администраторов;
  - это соответствует текущей логике проекта.

### Что обязательно нужно менять

#### Страница 4

Нужно убрать фразу:

- `Технология заказа кондитерского изделия со стороны клиента...`

Это явный кусок из чужого проекта и его нельзя оставлять вообще.

На что заменить:

- написать, что система предоставляет регистратору и администратору единый интерфейс для регистрации пациентов, оформления записи, просмотра приемов и ведения медицинских записей.

#### Страницы 5-6, раздел `1.1 Выбор инструментов`

Этот раздел нужно переписать почти полностью.

Что сейчас неверно:

- там описан `WPF`;
- там упор на `C#`;
- там текст местами написан под десктопную Windows-версию;
- есть странные формулировки про `кондитерские`, `постоянных клиентов`, `заказы на крупные мероприятия`.

Что оставить:

- абзацы про `PostgreSQL` и `pgAdmin` можно оставить как основу, но подредактировать стиль.

На что менять:

- вместо `WPF` написать, что текущая версия проекта реализована как кроссплатформенное веб-приложение;
- вместо `C#` и `Windows Presentation Foundation` указать `Node.js`, `HTML`, `CSS`, `JavaScript`;
- вместо формулировок про Windows написать, что система запускается через браузер на `macOS`, `Windows` и `Linux`;
- отдельно указать, что веб-формат был выбран для повышения переносимости и удобства демонстрации.

Готовая смысловая замена:

- `Для реализации проекта была выбрана веб-архитектура, включающая серверную часть на Node.js, базу данных PostgreSQL и клиентский интерфейс на HTML, CSS и JavaScript. Такой подход обеспечивает кроссплатформенность решения и позволяет запускать систему через браузер без привязки к конкретной операционной системе.`

#### Страницы 7-13, описание базы данных

Этот блок надо серьезно обновить, потому что в PDF описано только 5 таблиц, а в текущем проекте уже 9 сущностей.

Что нельзя оставлять как есть:

- `база данных, состоящая из пяти таблиц`;
- таблицу `user`, если у вас по факту используется `patient`;
- поля `policlinic_name`, `specialization`, `service_name` как просто строки внутри других таблиц;
- старую логику, где сущности не нормализованы.

На что менять:

- написать, что в текущем проекте реализовано 9 сущностей:
  - `patient`
  - `policlinic`
  - `specialization`
  - `doctor`
  - `service`
  - `doctor_service`
  - `doctor_availability`
  - `appointment`
  - `medical_record`
- отдельно подчеркнуть, что база данных нормализована;
- указать связи `1:M`, `M:N`, `1:1`;
- объяснить, почему `policlinic` вынесена в отдельную таблицу;
- объяснить, что `doctor_service` нужна для связи врача и услуги с ценой;
- объяснить, что `medical_record` хранит диагноз, лечение и примечания по конкретному приему.

Что удобно взять из текущего проекта:

- описание сущностей и связей из разделов `Описание базы данных`, `Связи между сущностями` и `Нормализация базы данных` этого `README`.

#### Страницы 14-17, листинги кода

Этот блок в текущем виде лучше не оставлять.

Почему:

- там старый `C#`-код;
- листинг `AddDoctorInformation` сопровождается текстом про пациента, что выглядит неаккуратно;
- код относится к старой WPF-версии, а показывать вы будете веб-приложение.

На что менять:

- вместо старых листингов C# вставить:
  - либо фрагменты `database_schema.sql`;
  - либо SQL-запросы на создание ключевых таблиц;
  - либо небольшие фрагменты из `server.js` с обработкой API;
  - либо примеры валидации из `public/app.js`.

Что лучше всего показать:

- `POST /api/patients` как пример регистрации пациента;
- `POST /api/appointments` как пример записи на прием;
- `POST /api/medical-records` как пример сохранения медицинской записи;
- SQL-фрагмент связи `doctor_service` и `appointment`.

#### Страницы 18-21, описание интерфейса и скриншоты

Этот блок нужно обновить под текущую веб-версию.

Что нельзя оставлять:

- старые скриншоты WPF;
- описание `MainWindow` и отдельных WPF-окон;
- формулировки про XAML-окна, если вы сдаете уже веб-приложение.

На что менять:

- вставить новые скриншоты из браузера;
- описать вкладки:
  - `Регистратура`
  - `Все записи`
  - `Поиск записи`
  - `Пациенты`
  - `Администрирование`
- отдельно отметить:
  - выбор пациента из списка;
  - выбор услуги из справочника;
  - валидацию телефона и ФИО;
  - человеческие статусы записи на русском языке;
  - добавление медицинской записи.

Готовая логика описания:

- сначала пользователь или администратор добавляет пациента;
- затем оформляет запись на прием через вкладку `Регистратура`;
- затем запись можно найти через `Поиск записи`;
- затем во вкладке `Все записи` можно открыть прием и заполнить медицинскую запись;
- во вкладке `Администрирование` можно добавлять врачей, услуги и доступные слоты.

#### Страница 22, заключение

Заключение нужно переписать под текущую реализацию.

Что нельзя оставлять:

- акцент на `WPF`;
- фразы про развитие навыков именно десктопной разработки;
- упоминание `Blend` и `XAML`, если это уже не основной результат проекта.

На что менять:

- написать, что была разработана кроссплатформенная информационная система;
- указать, что система включает PostgreSQL, серверную часть на Node.js и веб-интерфейс;
- подчеркнуть, что проект поддерживает регистрацию пациента, запись на прием, поиск записи и ведение медицинской записи;
- отметить, что база данных приведена к нормализованному виду.

Готовая смысловая замена:

- `В результате выполнения курсовой работы была разработана кроссплатформенная информационная система для поликлиники. В системе реализованы нормализованная база данных PostgreSQL, серверная логика для работы с данными и веб-интерфейс, доступный через браузер. Разработанное решение автоматизирует регистрацию пациентов, оформление записи на прием, поиск записей и ведение медицинских записей.`

#### Страница 23, библиографический список

Список источников тоже надо почистить.

Что лучше убрать:

- `PHP`, если он не используется;
- `Oracle MySQL Cloud Service`, если у вас PostgreSQL;
- случайные или слишком общие ссылки, которые не связаны с проектом.

Что можно оставить:

- учебники по базам данных;
- общие источники по проектированию БД.

Что стоит добавить:

- документацию PostgreSQL;
- документацию Node.js;
- справочные материалы по HTML/CSS/JavaScript;
- при желании документацию `MDN Web Docs`.

### Короткий чек-лист перед переделкой отчета

- везде заменить акцент с `WPF` на веб-приложение;
- убрать все следы чужой темы про кондитерку;
- обновить описание БД с 5 таблиц до 9 сущностей;
- заменить старые скриншоты интерфейса на скриншоты веб-версии;
- обновить листинги кода под текущую реализацию;
- переписать заключение под `PostgreSQL + Node.js + HTML/CSS/JavaScript`;
- почистить список литературы от неиспользуемых технологий.

## Возможные направления развития

Проект можно расширить следующими функциями:

- роли пользователей;
- авторизация;
- фильтрация записей по дате;
- история приемов пациента;
- удаление и редактирование записей;
- календарное отображение расписания;
- экспорт данных в отчетные формы.

## Итог

В результате выполнения проекта была разработана кроссплатформенная информационная система для поликлиники, включающая:

- нормализованную базу данных PostgreSQL;
- серверную логику на Node.js;
- веб-интерфейс для работы с пациентами, врачами, услугами и записями;
- модуль ведения медицинских записей;
- проверки корректности пользовательского ввода.

Проект демонстрирует практическое применение проектирования базы данных и разработки прикладной информационной системы для выбранной предметной области.
